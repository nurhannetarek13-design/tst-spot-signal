name: Mid Momentum Archive OOS V2

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "research/mid_momentum_archive_oos_v2.py"
      - ".github/workflows/mid-momentum-archive-oos-v2.yml"

permissions:
  contents: read

concurrency:
  group: mid-momentum-archive-oos-v2-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install --disable-pip-version-check numpy pandas
      - name: Syntax check
        run: python -m py_compile research/mid_momentum_archive_oos_v2.py research/mid_momentum_continuation_v1.py
      - name: Run archived-only OOS validation
        run: PYTHONPATH=. python research/mid_momentum_archive_oos_v2.py
      - name: Assert fail-closed output
        run: |
          python - <<'PY'
          import json
          p='validation/edges/mid-momentum-archive-oos-v2-latest.json'
          x=json.load(open(p))
          assert x['authorization']=='RESEARCH_ONLY'
          assert x['liveTrading'] is False
          assert x['automaticPromotion'] is False
          assert x['productionPromotionBlocked'] is True
          assert x['validationDesign']['entryTiming']=='NEXT_15M_OPEN'
          assert x['validationDesign']['sameBarTpSlResolution']=='SL_FIRST_PESSIMISTIC'
          assert len(x['targetsLoaded']) > 0
          print('mid momentum archive OOS fail-closed assertions passed')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: mid-momentum-archive-oos-v2-report
          path: validation/edges/mid-momentum-archive-oos-v2-latest.json
          retention-days: 14
