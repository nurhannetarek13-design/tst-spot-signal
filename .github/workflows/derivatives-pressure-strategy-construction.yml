name: Derivatives Pressure Strategy Construction

on:
  workflow_dispatch:
  push:
    branches: [fix/unified-execution-parity]
    paths:
      - "research/construct_derivatives_pressure_48h_strategy.py"
      - ".github/workflows/derivatives-pressure-strategy-construction.yml"

permissions:
  contents: read

jobs:
  construct:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run executable strategy construction
        run: python research/construct_derivatives_pressure_48h_strategy.py
      - name: Enforce research-only contract
        run: |
          python - <<'PY'
          import json, pathlib
          r=json.loads(pathlib.Path('validation/edges/derivatives-pressure-48h-strategy-construction.json').read_text())
          print(json.dumps(r,indent=2))
          if r.get('liveTrading') is not False: raise SystemExit('liveTrading must remain false')
          if r.get('authorization') != 'RESEARCH_ONLY': raise SystemExit('authorization must remain RESEARCH_ONLY')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: derivatives-pressure-strategy-construction
          path: validation/edges/derivatives-pressure-48h-strategy-construction.json
          if-no-files-found: error
