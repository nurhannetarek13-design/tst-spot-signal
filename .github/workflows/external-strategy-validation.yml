name: External strategy validation

on:
  workflow_dispatch:
  push:
    branches: [fix/unified-execution-parity]
    paths:
      - 'scripts/validate-external-strategies.mjs'
      - 'scripts/validate-bec-builtins.mjs'
      - 'production/strategies/**'
      - '.github/workflows/external-strategy-validation.yml'

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validate first external adapters
        run: node scripts/validate-external-strategies.mjs
      - name: Validate all BEC builtins
        run: node scripts/validate-bec-builtins.mjs
      - name: Upload validation reports
        uses: actions/upload-artifact@v4
        with:
          name: external-strategy-validation
          path: validation/external-strategies/*.json
