name: Derivatives Pressure Symbol Robustness

on:
  push:
    branches: [fix/unified-execution-parity]
    paths:
      - 'research/validate_derivatives_pressure_symbol_robustness.py'
      - '.github/workflows/derivatives-pressure-symbol-robustness.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run symbol robustness validation
        run: python research/validate_derivatives_pressure_symbol_robustness.py
      - name: Enforce research-only contract
        run: |
          python - <<'PY'
          import json, pathlib
          p=pathlib.Path('validation/edges/derivatives-pressure-48h-symbol-robustness.json')
          r=json.loads(p.read_text())
          print(json.dumps(r,indent=2))
          if r.get('liveTrading') is not False: raise SystemExit('liveTrading must remain false')
          if r.get('authorization') != 'RESEARCH_ONLY': raise SystemExit('authorization must remain RESEARCH_ONLY')
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: derivatives-pressure-symbol-robustness
          path: validation/edges/derivatives-pressure-48h-symbol-robustness.json
          if-no-files-found: error
