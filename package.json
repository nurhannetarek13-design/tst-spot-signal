{
  "name": "tst-spot-signal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node scripts/render-server.mjs",
    "check:render": "node --check scripts/render-server.mjs",
    "test": "node --test",
    "deploy": "wrangler deploy",
    "start:oracle": "node scripts/oracle-executor.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "5.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
