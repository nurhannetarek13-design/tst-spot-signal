import fs from 'node:fs/promises';
const BASES=['https://api.binance.com','https://api1.binance.com','https://api2.binance.com','https://data-api.binance.vision'];
const DISCOVERY=['BTCUSDT','ETHUSDT','BNBUSDT','SOLUSDT','XRPUSDT','DOGEUSDT','ADAUSDT','TRXUSDT','LINKUSDT','AVAXUSDT','DOTUSDT','LTCUSDT','BCHUSDT','NEARUSDT','UNIUSDT','AAVEUSDT','ETCUSDT','FILUSDT','ATOMUSDT','APTUSDT','ARBUSDT','OPUSDT','INJUSDT','SUIUSDT'];
const HOLDOUT=new Set(['XLMUSDT','HBARUSDT','ICPUSDT','ALGOUSDT','VETUSDT','THETAUSDT','RUNEUSDT','GRTUSDT']);
const LEADERS=['BTCUSDT','ETHUSDT','BNBUSDT','SOLUSDT'];const DAYS=1825,H=[48,72,120,168];
const TH={breadth50:0.70,impulse24:0.15,leader24:0.03,pos24:0.70};const GATE={mean:0.015,hit:0.55,mfeMae:2.0};
async function get(path){let last;for(const b of BASES){try{const r=await fetch(b+path,{signal:AbortSignal.timeout(20000)});if(r.ok)return await r.json();last=`${b}:${r.status}`;}catch(e){last=e.message;}}throw new Error(last||path);}
async function klines(symbol){const out=[];let cur=Date.now()-DAYS*86400000,now=Date.now();while(cur<now){const p=await get(`/api/v3/klines?symbol=${symbol}&interval=1h&limit=1000&startTime=${cur}`);if(!p.length)break;for(const k of p)out.push({t:+k[0],o:+k[1],h:+k[2],l:+k[3],c:+k[4],ct:+k[6]});const n=+p.at(-1)[6]+1;if(n<=cur)break;cur=n;if(p.length<1000)break;}return out.filter(x=>x.ct<now);}
const mean=a=>a.length?a.reduce((s,x)=>s+x,0)/a.length:0;function median(a){if(!a.length)return NaN;const x=[...a].sort((a,b)=>a-b),m=Math.floor(x.length/2);return x.length%2?x[m]:(x[m-1]+x[m])/2;}function ret(r,i,n){return i>=n?r[i].c/r[i-n].c-1:NaN;}
const rows=new Map();for(const s of DISCOVERY){if(HOLDOUT.has(s))continue;try{const r=await klines(s);if(r.length>=24*180){rows.set(s,r);console.error('downloaded',s,r.length)}}catch(e){console.error('skip',s,e.message)}}
const maps=new Map([...rows].map(([s,r])=>[s,new Map(r.map((x,i)=>[x.t,i]))]));const allTimes=[...new Set([...rows.values()].flatMap(r=>r.map(x=>x.t)))].sort((a,b)=>a-b);
const breadth=[];for(const t of allTimes){let above=0,pos=0,n=0;const leader=[];for(const [s,r] of rows){const i=maps.get(s).get(t);if(i==null||i<50)continue;const sma=mean(r.slice(i-49,i+1).map(x=>x.c));const r24=ret(r,i,24);if(!Number.isFinite(r24))continue;n++;if(r[i].c>sma)above++;if(r24>0)pos++;if(LEADERS.includes(s))leader.push(r24);}if(n>=8&&leader.length>=3)breadth.push({t,b50:above/n,pos24:pos/n,lead:median(leader)});}
const bMap=new Map(breadth.map((x,i)=>[x.t,i]));const events=[];for(let i=24;i<breadth.length;i++){const b=breadth[i],p=breadth[i-24];if(b.b50>=TH.breadth50&&(b.b50-p.b50)>=TH.impulse24&&b.lead>=TH.leader24&&b.pos24>=TH.pos24)events.push({t:b.t});}
const result={generatedAt:new Date().toISOString(),status:'RESEARCH_ONLY',protocol:'validation/round3-breadth-definition.json',category:'MARKET_BREADTH_AGGREGATED_LEAD_LAG',days:DAYS,thresholds:TH,eventCount:events.length,horizons:{}};
for(const h of H){const vals=[],mfes=[],maes=[];for(const e of events){for(const [s,r] of rows){if(LEADERS.includes(s))continue;const i=maps.get(s).get(e.t);if(i==null||i+1>=r.length||i+h>=r.length)continue;const entry=r[i+1].o,exit=r[i+h].c;if(!(entry>0&&exit>0))continue;const p=r.slice(i+1,i+h+1);vals.push(exit/entry-1);mfes.push(Math.max(...p.map(x=>x.h))/entry-1);maes.push(Math.abs(Math.min(0,Math.min(...p.map(x=>x.l))/entry-1)));}}const s={n:vals.length,meanGross:mean(vals),medianGross:median(vals),hitRate:vals.length?vals.filter(x=>x>0).length/vals.length:0,medianMFE:median(mfes),medianMAE:median(maes)};s.medianMfeMaeRatio=s.medianMAE===0?(s.medianMFE>0?Infinity:0):s.medianMFE/s.medianMAE;s.pass=s.meanGross>=GATE.mean&&s.medianGross>0&&s.hitRate>GATE.hit&&s.medianMfeMaeRatio>=GATE.mfeMae;result.horizons[h]=s;}
result.rawGatePassed=Object.values(result.horizons).some(x=>x.pass);result.verdict=result.rawGatePassed?'FREEZE_EVENT_AND_PROCEED_OOS':'REJECT_BEFORE_STRATEGY_CONSTRUCTION';
await fs.mkdir('validation',{recursive:true});await fs.writeFile('validation/round3-breadth-discovery.json',JSON.stringify(result,null,2));console.log(JSON.stringify(result,null,2));